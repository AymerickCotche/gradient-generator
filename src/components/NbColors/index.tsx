// == Import
import * as React from 'react';
import './styles.scss';

// == Composant
const NbColors = () => (
  <div id="nbColors">0 couleur(s) générée(s)</div>
);

// == Export
export default NbColors;
